# Radarmatic

### New and improved.

    git clone https://github.com/jstn/radarmatic.git --depth 1
    cd radarmatic
    bin/bundle install
    bin/yarn install
    bin/rails db:setup
    bin/rails server
